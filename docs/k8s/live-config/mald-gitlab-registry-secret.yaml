# Generated from command:
# kubectl create secret docker-registry registry.gitlab.com --docker-server=registry.gitlab.com --docker-username=gitlab-ci-token --docker-password=<gitlab personal access token> --docker-email=<your gitlab email>
apiVersion: v1
data:
  .dockercfg: # base64 encoded personal access token
kind: Secret
metadata:
  name: registry.gitlab.com
type: kubernetes.io/dockercfg
